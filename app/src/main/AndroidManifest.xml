<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 🌐 Internet (for images, maps, API) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 📍 Location (for Google Maps live location) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:label="SantheConnect"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SantheConnect">

        <!-- 🗺️ Google Maps API -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD8Y2b82IxWxIXnhW5TYUsQsCeTjyASvfs"/>

        <!-- 🚀 Splash Screen (Android 12+) -->
        <meta-data
            android:name="android.windowSplashScreenBackground"
            android:resource="@color/black"/>

        <meta-data
            android:name="android.windowSplashScreenAnimatedIcon"
            android:resource="@mipmap/ic_launcher"/>

        <!-- 📱 Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SantheConnect">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>

</manifest>